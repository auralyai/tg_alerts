[project]
name = "tg-alerts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
]

[project.optional-dependencies]
telegram = [
    "pyrofork>=2.3.69",
    "tgcrypto>=1.2.5",
]
